import { Zipper } from "../../src/torrent-to-pixeldrain/components/zipper";
describe("Testing Zipper functions", () => {
    test("Zip a directory", () => {
        let zipper = new Zipper()
    })

    test("Zip a file", () => {

    })
})